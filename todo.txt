- We can save to a TLV store or a JSON store, selected at compile-time. But
  we need to automate testing both of these builds.
- Allow an owned array element to be identified by a name instead of an index.
- Create iterators that can iterate across item descriptors and items themselves.
- Produce quality documentation from doxygen.
- Unit tests to verify that a user's initialization of cfg_mgr is correct, i.e.
  it's a generic test, that verifies some things that are done in my_cfg.cpp.
- Extend utils to unsigned int, strings, hexstrings, etc.
- Can we avoid saving the unused parts of strings to TLV?  Descriptor could
  contain a ptr for a getLen fn, which is NULL in most cases, but the user could
  install strlen for strings.  If NULL, the default is to use the full length of the item
  from the descriptor.
- Having an ID in a counter descriptor is not needed, since it's not saved to TLV.
  The same applies to any non-persistent items like stats, that are not saved 
  in NVRAM.
- An ID is also not needed for JSON store, since it uses the name as ID.
- Provide context-sensitive help in a way that could be used for "tabbing out" input strings.
  Step 1: respond to inputs without a carriage return.
- Strategy for making fewer methods public: do we need friends, or interface classes...
- Make C-friendly: a C-friendly wrapper?
- Should I be using malloc, or new?
- Is there superfluous info in the TLV files?  At least for simple types, if I know the type
  I already know the length. No, we need this for migration -- we can skip over an obsolete
  entry if the size is in the file, even though we don't know the type because the
  entry is obsolete.

- Set more than one component of a composite in a single command?
- Display members of an array by giving array name without index -- or maybe if application
  developer want this he should just wrap the array in a struct.
- An example of a bitmap item, just to show how the set and prt functions look

- YAML schemas to validate descriptor and ID files?
- If a counter aggregate should not be included in its container
  (except as referenced by the counted aggr), how do we indicate that?
  A flag that says "don't include me", which is false by default?
- File headers, footers
- Should YAML config file specify the names of the output file(s)?
- Warn user of incorrect configuration:
  - Owned items with count > 1 need counters (what happens if we violate this?)
  - Counters shouldn't have set or setdef (what happens if we add and use them?)
  - Counter should be big enough to accomodate max count

Restrict application code from writing to config?  Currently it's temptingly
easy...

- howto should be written in html, probably in a way that can be integrated with
  Doxygen.

- The /export directory is not complete, since we also export cm_setdef_null and cm_util...
  Need to clarify what the public interface is, precisely.
- Add a specific index, e.g. "add user 0" -- is this worth doing?  It would allow us to
  insert entries, in lists where the order matters, e.g. encodes priority.
- Default values don't need to be saved.  Is that worth doing?  Risky?  What if
  defaults change?
- The code generator requires the counter for an owned item to exist in the same container
  as the owned item. (And it is always printed, using the default print method).
  Changing this would not be easy, since the generator assumes all items are part of
  one hierarchy.
- The code generator can't detect that a structure can be re-used if the same elements
  are repeated in different places. Is this worth doing? Should we change the format
  if cfg.yaml to support this? If cfg.yaml is too complex, we might as well just
  code the C++ directly!
  
